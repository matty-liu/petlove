
# matches = {}
# User.all.each do |user|
#   compatibility_value = 50;
#   user.answers.each do |user_answer|
#     compatibility_value += user_answer.compatibility
#   end
#   matches[user_id] = compatibility_value
# end
